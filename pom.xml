<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>
  <groupId>example</groupId>
  <artifactId>native-demo</artifactId>
  <version>1.0.0</version>
  <properties>
    <maven.compiler.release>21</maven.compiler.release>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    <native.image.name>native-demo</native.image.name>
  </properties>
  <build>
    <finalName>${native.image.name}</finalName>
    <plugins>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-compiler-plugin</artifactId>
        <version>3.13.0</version>
      </plugin>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-jar-plugin</artifactId>
        <version>3.4.2</version>
        <configuration>
          <archive><manifest><mainClass>example.Main</mainClass></manifest></archive>
        </configuration>
      </plugin>
    </plugins>
  </build>
  <profiles>
    <!-- 普通 mvn package 仍可生成 JAR；加 -Pnative 才进行原生编译。 -->
    <profile>
      <id>native</id>
      <build>
        <plugins>
          <plugin>
            <groupId>org.graalvm.buildtools</groupId>
            <artifactId>native-maven-plugin</artifactId>
            <version>0.10.6</version>
            <extensions>true</extensions>
            <executions>
              <execution>
                <id>native-package</id>
                <phase>package</phase>
                <goals><goal>compile-no-fork</goal></goals>
              </execution>
            </executions>
            <configuration>
              <imageName>${native.image.name}</imageName>
              <mainClass>example.Main</mainClass>
              <!-- 复用第三方库的元数据；自己动态访问的类/资源仍需自己声明。 -->
              <metadataRepository><enabled>true</enabled></metadataRepository>
              <buildArgs>
                <!-- 无法原生编译就失败，不生成依赖 JVM 的 fallback 程序。 -->
                <buildArg>--no-fallback</buildArg>
                <!-- 使用通用 CPU 指令基线，避免编译机特性限制部署机器。 -->
                <buildArg>-march=compatibility</buildArg>
              </buildArgs>
            </configuration>
          </plugin>
        </plugins>
      </build>
    </profile>
  </profiles>
</project>
